#include "handle_requests_kernel.h"

void handle_requests(int socket_kernel, t_log *logger_aux)
{
   /*switch ()
   {
   case ABRIR_ARCHIVO:
      open_file();
      log_info(logger_aux,"archivo abierto");
      break;

   case CREAR_ARCHIVO:
      create_file();
      log_info(logger_aux,"archivo creado");
      break;

   case TRUNCAR_ARCHIVO:
      truncate_file();
      log_info(logger_aux,"archivo truncado");
      break;

   case LEER_ARCHIVO:
      read_file();
      log_info(logger_aux,"archivo leido");
      break;

   case ESCRIBIR_ARCHIVO:
      write_file();
      log_info(logger_aux,"archivo escrito");
      break;


   default:
      log_info(logger_aux,"error codigo operacion");
      break;
   }*/
}
